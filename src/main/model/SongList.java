package model;

import java.util.*;

// Represent a list of all music
public class SongList {
    
    // Instantiate a list that stores all the music
    public SongList() {

    }

    // REQUIRES: index >= 0 && index < list.size()
    // MODIFIES: this
    // EFFECT: add music at the specific index to the music list
    public boolean addMusic(int index) {
        // stub
        return false;
    }

    // REQUIRES: index >= 0 && index < list.size()
    // MODIFIES: this
    // EFFECTS: delete the music at the specific index from the music list
    public boolean deleteMusic(int index) {
        // stub
        return false;
    }

    // EFFECTS: display all the music from the list
    public String displayList() {
        return "";
    }


    // EFFECTS: return the size of the list
    public int getSize() {
        return 0;
    }


}
